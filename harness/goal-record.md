# Goal record

## Objective

Complete phase 6: reset the first-person dungeon projection to deterministic nested portal frames, add geometry tests, and capture the ten required browser fixtures.

## Current phase

Phase 6 — nested portal-frame projection reset.

## Constraints

TypeScript, Vite, Phaser presentation, framework-independent pure simulation, deterministic seeded state, grid coordinates, cardinal facing, original presentation.

## Stopping conditions

Phase-6 acceptance rows all have evidence: projection tests, simulation regression tests, typecheck/build, and actual browser-visible fixture screenshots. Art, textures, props, particles, lighting, new UI, monsters, animation, and simulation/topology redesign remain out of scope.

## Evidence index

See `harness/build-log.md` and `harness/build/phase-6-acceptance.md`.
